import 'package:flutter/material.dart';

const kLightPrimaryColor = Color(0XFF4F964F);
const kPrimaryBlackColor = Color(0XFF0D1C0D);
final kYellowTextColor = Colors.yellow.shade700;
final kRedTextColor = Colors.red.shade700;
final kLightHighlightYellowColor = Colors.yellow.shade100;
final kLightHighlightRedColor = Colors.red.shade100;
const kLightBorderColor = Color(0xFFB0BEC5);
const kSubTextColor = Color(0xFF455A64);
final kLightHighlightGreenColor = Colors.green.shade100;
final kLightHighlightBackGroundGreenColor = Colors.green.shade100;
final klightGreenTextColor = Colors.green.shade700;

// 🎨 Dark Mode Colors
const kDarkPrimaryColor = Color(0xFF2E7D32); // أخضر غامق كـ primary
const kPrimaryWhiteColor = Color(0xFFECEFF1); // أبيض فاتح للنصوص
const kDarkYellowTextColor = Colors.yellowAccent; // أصفر هادي
const kDarkRedTextColor = Colors.redAccent; // أحمر غامق هادي
final kDarkHighlightYellowColor = Colors.yellowAccent.withValues(
  alpha: 0.2,
); // أصفر شفاف للـ highlight
final kDarkHighlightRedColor = Colors.redAccent.withValues(
  alpha: 0.2,
); // أحمر شفاف للـ highlight
const kDarkBorderColor = Color(0xFF37474F); // رمادي غامق كـ border
const kDarkSubTextColor = Color(0xFF90A4AE); // رمادي فاتح للنصوص الثانوية
final kDarkHighlightGreenColor = Colors.greenAccent.withValues(
  alpha: 0.2,
); // أخضر فاتح شفاف
const kDarkHighlightBackGroundGreenColor = Color(
  0x334CAF50,
); // خلفية خضراء فاتحة شفافة
final kDarkTextGreenColor = Colors.greenAccent;
